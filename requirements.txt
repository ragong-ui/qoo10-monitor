requests>=2.31.0
truststore>=0.10.4       # Use the Windows trusted root store for HTTPS
python-dotenv>=1.0.0
streamlit>=1.35.0
pandas>=2.0.0

# Compliance Briefing (optional — install as needed)
feedparser>=6.0.0         # e-Gov / CAA / NITE RSS parsing
anthropic>=0.30.0         # LLM summarization (optional)
openai>=1.30.0            # LLM summarization (optional)
beautifulsoup4>=4.12.0    # HTML parsing fallback for CAA/NITE
pytest>=8.0.0             # Tests
